import { InvolvedPage, involvedMetadata } from '@/components/site/pages/InvolvedPage';

export const metadata = involvedMetadata('fr');

export default function Page() {
  return <InvolvedPage locale="fr" />;
}
